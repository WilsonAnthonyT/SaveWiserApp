import 'package:flutter/material.dart';

class PlanningsPage extends StatelessWidget {
  const PlanningsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Daily Plannings Page'));
  }
}
